import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.edumanagepro.school',
  appName: 'EduManage Pro',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
